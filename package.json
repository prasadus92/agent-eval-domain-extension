{
  "name": "agent-eval-domain-extension",
  "version": "1.0.0",
  "description": "Extending tau-bench (Sierra Research) into a Food Delivery domain as RL-ready training data.",
  "private": true,
  "scripts": {
    "render": "node slides/render_pdf.js",
    "open": "open slides/slides.html"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  }
}